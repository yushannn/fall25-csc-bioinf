name: Week 2 - Bio.motifs Port

on:
  push:
    branches: [ main ]
    paths:
      - 'week2/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'week2/**'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Install BioPython
      run: |
        pip install biopython numpy
        
    - name: Install Codon
      run: |
        /bin/bash -c "$(curl -fsSL https://exaloop.io/install.sh)"
        echo 'export PATH="$HOME/.codon/bin:$PATH"' >> $GITHUB_ENV
        
    - name: Verify installations
      run: |
        export PATH="$HOME/.codon/bin:$PATH"
        python --version
        python -c "import Bio; print('BioPython version:', Bio.__version__)"
        codon --version
        
    - name: Run comprehensive tests
      run: |
        cd week2
        python test_comprehensive.py
        
    - name: Run performance benchmarks
      run: |
        cd week2
        python benchmark_performance.py
        
    - name: Test original compatibility (if BioPython available)
      run: |
        cd week2
        python test.py || echo "Original test skipped (expected in CI environment)"